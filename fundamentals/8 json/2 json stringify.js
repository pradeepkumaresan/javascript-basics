const user = {'name': 'abc', 'email': 'abc@email.com', 'age': 28}

console.log(JSON.stringify(user)); 

/**
 * when you want to send data from a client to a server, 
 * you need to convert the data to a string.
 */